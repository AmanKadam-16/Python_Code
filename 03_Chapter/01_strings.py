# string in Python
# standard def. Strings are nothing but sequence of 
# character enclosed within single or double quotes or even sometimes in triplets
# Also string as a datatype are Immuatable by nature

sample_text : str = "umbrella"
# we can find the lenght of string using len() function
print(sample_text)
print(len(sample_text))
